/**
 *  P1: Convert to a number to absolute string
 */

function absolute(n){
    return "(" + Math.abs(n) + ")";
}

console.log(absolute(-1));
console.log(absolute(0));
console.log(absolute(1));
console.log(absolute(10));


/**
 *  P2: Calculate the total amount
 */

function calculateTotal(items,options) {
  let total= getItemTotal(items)
  total= getPayableAmount(items,options)
  return total;
}

 function getItemTotal(items){
  let total = 0;
  items.forEach((item) => {
    total += item.price * item.quan;
  });
  return total
}

function getPayableAmount(items,options){
  total -= tootal * (options.dis || 0);
  total *= 1.1;
  total += (options.ship || 5);
  return total
}

const testItems = [
  { price: 15, quan: 2 },
  { price: 20, quan: 1 },
  { price: 5, quan: 4 },
];

console.log(calculateTotal(testItems, {}));
console.log(calculateTotal(testItems, { ship: 0 }));
console.log(calculateTotal(testItems, { dis: 0.75 }));
console.log(calculateTotal(testItems, { ship: 12 }));