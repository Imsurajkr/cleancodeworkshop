/**
 *  P1: Convert to a number to absolute string
 */

function convertToAbsoluteString(number){
    return "(" + Math.abs(number) + ")";
}

print(convertToAbsoluteString(-1));
print(convertToAbsoluteString(0));
print(convertToAbsoluteString(1));
print(convertToAbsoluteString(10));

function print(message){
  console.log(message);
}

/**
 *  P2: Calculate the total amount
 */
const defaultShipmentCharge = 5;
const defaultDiscount = 0;

function calculateTotalAmount(items, options) {
  let totalAmount = 0;
  items.forEach((item) => {
    totalAmount += item.price * item.quantity;
  });
  totalAmount = applyDiscount(totalAmount,discount);
  totalAmount = applyShipmentCharge(totalAmount,shipmentCharge);
  return totalAmount;
}

function applyDiscount(amount,discount){
  let discount =  amount * (options.discount || defaultDiscount);
  amount = amount - discount;
  return amount;
}

function applyShipmentCharge(amount,charge){
  let shipmentCharge = (options.shipmentCharge || defaultShipmentCharge);
  amount = amount + shipmentCharge;
  return amount;
}

const testItems = [
  { price: 15, quantity: 2 },
  { price: 20, quantity: 1 },
  { price: 5, quantity: 4 },
];

// console.log(calculateTotal());
print(calculateTotalAmount(testItems, {}));
print(calculateTotalAmount(testItems, { shipmentCharge: 0 }));
print(calculateTotalAmount(testItems, { discount: 0.75 }));
print(calculateTotalAmount(testItems, { shipmentCharge: 12 }));